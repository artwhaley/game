# Orchestrator prompt — Conversation Performance V1

Execute only after the user authorizes implementation. This packet revision itself is documentation only. Read agents.md, root README, current GraphWorkbench documentation and Docs/CONTENT-DATABASE-VERSIONING.md, then this packet's README, ARCHITECTURE-CONTRACT, TICKET-STACK and Docs/UnityPerformance/BUILD-PLAN.md.

Current packet text supersedes previous packet commits. Do not revive the prior eleven-ticket scope.

## Deliverable

A real WPF-authored SQLite Card executes Perform("Playful Tease"), presents two selected dialogue lines with automatic compatible acting refresh, and waits for Continue on a real/representative Unity rig. Core plans staging and picks ingredient IDs; Unity executes and retains coherent presentation. Repeat can vary acting. Adding a compatible gesture expands the same event/Card without edits.

Finish this editor-playable slice before driven motion, voice, player posing, comprehensive diagnostics or standalone packaging.

## Source and architecture instructions

Use Content/GameContent.db → existing Game.Content.Sqlite → GameContentDefinition → Game.Core → host services. No game-content JSON, closure exporter, duplicate action/graph transport or stale publication protocol.

Ticket 00 must prove a compatible Unity SQLite provider and consistent read-only snapshot loading. New dependencies require approval. A real incompatibility requires a precise reproduction and review before changing architecture; do not quietly substitute a different pipeline. Continue independent work around blockers, but never mark the visual milestone complete without the real content path.

Physical portable source lives under Assets/Scripts/Portable and is linked into DotNet. Do not duplicate it. Current source overrides historical milestone counts and obsolete action descriptions.

Fix ActionExecutor through explicit Activity versus ControlOrYield classification. Preserve all existing yield/transfer semantics and blocking activity behavior; no growing exception lists.

Core owns event resolution, factored anchor/posture plans, compatibility, semantic selections/RNG and requested/committed state. Use async IPerformanceHost requests/readiness. Core has no delta-time tick, mixer, continuous ambience scheduler or speech queue. Unity owns rendering, blends, IK, gaze and visual lifetime.

## Authoring instructions

V1 has one new action, Perform(eventId), with no overrides. Events have small semantic queries, staging/optional posture-location constraints and automatic refresh at dialogue start. There is no public refresh action or parallel Mood state.

WPF/SQLite owns Performance Tag definitions separately from Card/Dialog Tags. Unity automatically reads those IDs/names; ingredient membership and compatibility remain Unity-owned. Generated PresentationCatalog is read-only to WPF and is the only new exchange artifact.

Make normal gesture intake sparse: clip, tags, verified posture and Enabled; standard mask/name/ID/technical details default or infer. Optional exceptions exist only where demonstrated.

Factor state and reuse operations. The six bedroom locations are fixtures, not Core enums or an authored Cartesian transition network.

Choose animation technology after the real-rig spike. Do not mandate PlayableGraph in advance or add packages speculatively.

## Execution and checks

Follow tickets 00–05 and their source context, acceptance and guardrails. Announce exact files before each batch. User approval of this execution packet provides standing authorization under agents.md rules 2/5, including coherent batches exceeding five files; dependency approval, content safety, three-strikes and verification rules remain.

Keep root README current after features. Checkpoint canonical DB according to repository policy; never copy a live database file. Commit coherent milestones, preserve unrelated backup files and push only with authorization.

Use the normal Session selector/VM for visual testing. A one-eligible-Card Session allows Play/Repeat without a second interpreter. Load committed SQLite content on each repeat while retaining the scene. Fail current reloads visibly. Do not skip unsupported actions or silently replay stale state.

Keep automatic dialogue refresh attached to actual presentation boundaries without changing dialogue selection order. Test delayed/nonblocking requests and stale callbacks narrowly; do not build a general scheduler. Persistent acting stays outside WaitForAll. Integrate cleanup and faults into the existing host/engine lifecycle.

Run targeted Core/persistence/editor tests and verify Unity visually. Launch and leave the affected app running after application edits. Report absent assets/provider limitations honestly; simulated host tests do not prove visuals.

## Stop at V1

Ticket 04 supplies the first integrated proof. Ticket 05 fixes observed workflow/visual problems and measures gesture intake/content reuse. Present V1 for acceptance, with runnable scene, provider setup, test results and short writer/artist instructions.

Phase 2 needs a separate reviewed packet. Do not prebuild scalar drivers/funscript, curve storage, pelvis driver channels, voice/lipsync, player-state actions, broad dashboards or packaging. Never add takes, per-line clip assignments, persisted random combinations or authored choreography tracks. A read-only Nodify plan view is allowed; future semantic behavior graphs may be considered from actual authoring evidence.
